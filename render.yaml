services:
  - type: web
    name: inspectiq-backend
    env: python
    buildCommand: pip install fastapi uvicorn
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: false